machine:
  services:
    - docker

dependencies:
  pre:
    - mkdir $PWD/.docker-credentials
    - echo -n $DOCKER_CERT_BZ2 | base64 --decode | tar -tj -C $PWD/.docker-credentials

deployment:
  production:
    branch: master
    commands:
      - docker-compose ps
#      - bash ./deploy.sh
